import { HomeFlashShowDirective } from './home-flash-show.directive';

describe('HomeFlashShowDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new HomeFlashShowDirective();
  //   expect(directive).toBeTruthy();
  // });
});

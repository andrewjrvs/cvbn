import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'us.cvbn.website',
  appName: 'cvbn',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
